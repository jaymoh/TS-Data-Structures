class QuickSort {

  private arr: number[]

  constructor(arr: number[]) {
    this.arr = arr
  }

  // arrange the array in ascending order
  quickSort(): number[] { // Big O: O(n log n) - logarithmic


    return this.arr
  }

  quickSortPartition(low: number, high: number): number {

    return 0
  }

}